[package]
name = "fem-tests"
version.workspace = true
authors.workspace = true
edition.workspace = true
repository.workspace = true
description.workspace = true

# This crate has no library — it exists purely to hold integration tests
# that exercise the full sparse → solvers → fem_core pipeline against
# known analytical FEM solutions.

[dev-dependencies]
sparse   = { path = "../sparse" }
solvers  = { path = "../solvers" }
fem_core = { path = "../fem-core" }