[package]
name    = "solvers"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = { workspace = true }
miette = { workspace = true }
sparse    = { path = "../sparse" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
faer      = "0.16"

# ── Benchmarks ────────────────────────────────────────────────────────────────

[[bench]]
name    = "cholesky_bench"
harness = false